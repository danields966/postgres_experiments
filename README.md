# postgres_experiments

### To run container, please use commands:

#### Windows:

    $env:TASK = 1
    docker-compose up --abort-on-container-exit

#### Unix:

    export TASK = 1
    docker-compose up --abort-on-container-exit
